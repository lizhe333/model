"""Model3-derived direct action-regression ablation."""

# Keep package import lightweight so the validated launcher can run under the
# host Python before the training command enters the Torch-enabled Conda env.
